# Instrucciones
1. Ejecutar `npm install` para instalar dependencias.
2. Ejecutar `npm run test:web` para correr los tests web.
3. Ejecutar `npm run test:apis` para correr los tests apis.
4. Ejecutar `npm run report:allure` para abir el reporte unificado.